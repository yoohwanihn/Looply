# =============================================
# Looply - 환경변수 설정
# =============================================

# ── PostgreSQL ────────────────────────────────
POSTGRES_DB=sns_db
POSTGRES_USER=sns_user
POSTGRES_PASSWORD=changeme_db_password

# ── Redis ─────────────────────────────────────
REDIS_PASSWORD=changeme_redis_password

# ── JWT ───────────────────────────────────────
JWT_SECRET=changeme_jwt_secret_must_be_at_least_256bits_long_string_here
JWT_ACCESS_EXPIRY=3600000
JWT_REFRESH_EXPIRY=604800000

# ── Spring Boot ───────────────────────────────
SPRING_PROFILES_ACTIVE=dev

# ── MinIO ─────────────────────────────────────
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_PUBLIC_URL=http://localhost:9100

# ── SonarQube ─────────────────────────────────
SONAR_JDBC_PASSWORD=changeme_sonar_password

# ── Jenkins ───────────────────────────────────
JENKINS_ADMIN_PASSWORD=admin
GITEA_ACCESS_TOKEN=a4364341000e8b1a3cfc32eba9838e9ec36408cb

# ── Docker 내부 네트워크 호스트 (application.yml에서 직접 참조) ──
# Docker 컨테이너 안에서 각 서비스에 접근할 때 사용하는 이름
# 로컬 직접 실행 시에는 아래 값을 localhost로 변경하거나 env 변수를 설정하지 않으면 기본값(localhost)이 사용됨
DB_HOST=db
REDIS_HOST=redis
REDIS_PORT=6379
MINIO_ENDPOINT=http://minio:9000
