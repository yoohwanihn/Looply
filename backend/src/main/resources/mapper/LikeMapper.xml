<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nt.sns.post.mapper.LikeMapper">
    <select id="exists" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM likes WHERE user_id = #{userId} AND post_id = #{postId})
    </select>
    <insert id="insert">
        INSERT INTO likes (user_id, post_id) VALUES (#{userId}, #{postId})
    </insert>
    <delete id="delete">
        DELETE FROM likes WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>
</mapper>
