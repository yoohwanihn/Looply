<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nt.sns.mention.MentionMapper">

    <insert id="insertPostMention">
        INSERT INTO mentions (post_id, mentioned_user_id) VALUES (#{postId}, #{mentionedUserId})
        ON CONFLICT DO NOTHING
    </insert>

    <insert id="insertCommentMention">
        INSERT INTO mentions (comment_id, mentioned_user_id) VALUES (#{commentId}, #{mentionedUserId})
        ON CONFLICT DO NOTHING
    </insert>

    <delete id="deleteByPostId">
        DELETE FROM mentions WHERE post_id = #{postId}
    </delete>

</mapper>
